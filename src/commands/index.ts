import history from './history-cmd'
import install from './install-cmd'
import list from './list-cmd'
import repo from './repo-cmd'

const helm = {
  history,
  install,
  list,
  repo
}

export default helm
